# 收录标准

这个列表的目标不是「尽可能多」，是**每一个条目都能说清楚为什么值得看**。

## 一条项目要进列表，需要满足

1. **它真的和 Jev 有关** —— 不是名字里带 Jev，核心链路里真的用到。
2. **能写出一句具体的推荐理由** —— 说清楚「它是干什么的、为什么它而不是同类另一个」。
   🔴 **写不出这句的，不进列表。** 只列名字的清单，GitHub 搜索一秒钟就能生成一份。
3. **仓库还活着** —— 已删除或已归档会被脚本自动标出来（见下）。

## 怎么提交

开一个 issue，写清楚：仓库地址、一句话理由、以及你把它放在哪一类。

## 每一条的检查项

- [ ] **仓库真实存在** —— `scripts/fetch_stars.py` 会对每个仓库打一次 API。
      🔴 拿不到的直接报错出来，**这条是静默失败的重灾区**：仓库没了，
      页面上徽章只会显示 `repo not found`，如果没人顺手去查，它会一直躺在那儿。
- [ ] **推荐理由不是从仓库名或描述猜的** —— 得真看过。
- [ ] **星数和日期⛔ 不手写** —— README 里放的是 shields.io 的实时徽章，
      排序表由 `data/stars.json` 生成。手写的数字第一个月就会错。

## 什么不收

- 🔧 **工具本身**（那是另一个赛道）
- 只改了名字、内容还是官方文档复制的仓库
